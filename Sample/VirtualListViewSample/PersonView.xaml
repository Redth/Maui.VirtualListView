<?xml version="1.0" encoding="UTF-8"?>
<xct:VirtualViewCell
	xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:xct="clr-namespace:Xamarin.CommunityToolkit.UI.Views"
	x:Name="self"
	x:Class="VirtualListViewSample.PersonView">
	<xct:VirtualViewCell.View>
		<StackLayout
			Spacing="0"
			BackgroundColor="{Binding Source={x:Reference self}, Path=IsSelected, Converter={StaticResource selectedColorConverter}}">

			<BoxView
				HorizontalOptions="FillAndExpand"
				HeightRequest="5"
				BackgroundColor="Lime" 
				IsVisible="{Binding Source={x:Reference self}, Path=IsNotFirstItemInSection}" />

			<Frame BackgroundColor="LightGray" HasShadow="False" CornerRadius="14" Margin="10,5,10,5" Padding="10">
				<StackLayout HorizontalOptions="FillAndExpand">
					<Label Text="Person" FontSize="Title" />
					<Label Text="{Binding FirstName}" />
					<Label Text="{Binding LastName}" />
					<Label Text="{Binding Email}" MaxLines="20" />
				</StackLayout>
			</Frame>

		</StackLayout>
	</xct:VirtualViewCell.View>
</xct:VirtualViewCell>