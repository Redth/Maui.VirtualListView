<?xml version="1.0" encoding="UTF-8"?>
<ContentView
	xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	HorizontalOptions="FillAndExpand"
	xmlns:xfslimlistview="clr-namespace:XFSlimListView"
	x:Class="SlimListViewSample.PersonView">
	<ContentView.Resources>
		<ResourceDictionary>
			<xfslimlistview:PositionData x:Key="position" />
		</ResourceDictionary>
	</ContentView.Resources>
	<ContentView.Content>
		<StackLayout Spacing="0">

			<BoxView
				HorizontalOptions="FillAndExpand"
				HeightRequest="5"
				BackgroundColor="Lime" 
				IsVisible="{Binding Source={StaticResource position}, Path=IsNotFirstItemInSection}" />
			
			<Frame BackgroundColor="LightGray" HasShadow="False" CornerRadius="14" Margin="10,5,10,5" Padding="10">
				<StackLayout HorizontalOptions="FillAndExpand">
					<Label Text="Person" FontSize="Title" />
					<Label Text="{Binding FirstName}" />
					<Label Text="{Binding LastName}" />
					<Label Text="{Binding Email}" MaxLines="20" />
				</StackLayout>
			</Frame>

		</StackLayout>
	</ContentView.Content>
</ContentView>